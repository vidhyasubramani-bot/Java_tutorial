# 📘 Java Learning Journey – Day 2

## 📅 Day

**Day 2**

## 📚 Topics Covered

### 1. Arithmetic Operators

Used to perform mathematical calculations.

Operators:

* `+` (Addition)
* `-` (Subtraction)
* `*` (Multiplication)
* `/` (Division)
* `%` (Modulus)

**Example:**

```java
int a = 10, b = 5;
System.out.println(a + b);
System.out.println(a - b);
System.out.println(a * b);
System.out.println(a / b);
System.out.println(a % b);
```

---

### 2. Assignment Operators

Used to assign and update values.

Operators:

* `=`
* `+=`
* `-=`
* `*=`
* `/=`
* `%=`

**Example:**

```java
int x = 10;
x += 5;
System.out.println(x);
```

---

### 3. Logical Operators

Used to combine multiple conditions.

Operators:

* `&&` (Logical AND)
* `||` (Logical OR)
* `!` (Logical NOT)

**Example:**

```java
int age = 20;

System.out.println(age > 18 && age < 60);
System.out.println(age < 18 || age > 60);
System.out.println(!(age == 20));
```

---

### 4. Bitwise Operators

Operate directly on binary values.

Operators:

* `&`
* `|`
* `^`
* `~`
* `<<`
* `>>`

**Example:**

```java
int a = 5;
int b = 3;

System.out.println(a & b);
System.out.println(a | b);
System.out.println(a ^ b);
System.out.println(a << 1);
System.out.println(a >> 1);
```

---

### 5. Conditional (Ternary) Operator

A shorthand alternative to the `if-else` statement.

Syntax:

```java
condition ? value1 : value2;
```

**Example:**

```java
int num = 25;
String result = (num % 2 == 0) ? "Even" : "Odd";
System.out.println(result);
```

---

### 6. If-Else Statement

Used to make decisions based on conditions.

**Example:**

```java
int marks = 75;

if (marks >= 50) {
    System.out.println("Pass");
} else {
    System.out.println("Fail");
}
```

---

## 💻 Practice Problems Solved

* Addition of two numbers
* Basic calculator using arithmetic operators
* Check Even or Odd
* Check Positive, Negative, or Zero
* Find the Largest of Two Numbers
* Simple If-Else Programs
* Bitwise Operator Practice
* Left Shift and Right Shift Operations
* Assignment Operator Examples
* Logical Operator Examples

---

## 🎯 Key Learnings

* Learned different types of Java operators.
* Understood operator precedence and expressions.
* Performed arithmetic calculations.
* Used logical operators to evaluate conditions.
* Learned binary operations using bitwise operators.
* Implemented decision-making using `if-else`.
* Used the ternary operator for concise conditional expressions.
* Improved problem-solving skills with basic Java programs.

---

## 🚀 Next Goal

Learn:

* `switch` statement
* Loops (`for`, `while`, `do-while`)
* Pattern printing
* Nested loops
* Break and Continue statements

---

## 🛠️ Language

* Java

## 💻 IDE

* Visual Studio Code / IntelliJ IDEA

---

**Day 2 Complete ✅**
